version: '3'
services:
  my-container:
    build: .
    image: ddpm-project
    container_name: ddpm-container
    stdin_open: true # Keeps stdin open
    tty: true        # Allocates a pseudo-TTY
    volumes:
      - .:/app

